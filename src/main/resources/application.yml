spring:
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:local}
    group:
      local: oauth,swagger,redis,actuator,elasticsearch
      prod: oauth,redis,actuator,elasticsearch

app:
  testdata:
    enabled: ${TESTDATA_BOOTSTRAP_ENABLED:false}
    retry-delay-ms: ${TESTDATA_BOOTSTRAP_RETRY_DELAY_MS:10000}
